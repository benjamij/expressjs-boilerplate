const axios = require('axios');
const KJUR = require('jsrsasign');
const ServiceAccount = require('../../auth/models/serviceAccount');

// Sample helper
module.exports = new function () {
    _token: null,
    this.doStuff = (param1, param2, param3) => {
        // Do stuff
    };

};
